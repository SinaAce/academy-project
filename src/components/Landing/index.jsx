import React from "react";
import Header from "./Header/Header";

const Landing = () => {
  return (
    <div className="w-full h-full relative -mt-[50px] -z-10">
      <Header />
    </div>
  );
};

export default Landing;
